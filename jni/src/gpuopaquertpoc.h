//select frame
// compose next and previous with alpha 
// ( in stream access texture )
// compose canvas with current
// blit frame to fb blit both transp prev and next 

//step 1 : compose prev next current from surf

//step 2 : array of 10 frames, next prev save functioning

namespace opaqueRTPOC{
void gpuopaquertpoc();
}