void testswblit();


// #include "testsw.h"
// #include "SDL.h"
// void testswblit(){
	// SDL_Window * win = NULL;
	// SDL_Surface * screen = NULL;

	// SDL_Surface * canvas = NULL;
	// SDL_Init(SDL_INIT_EVERYTHING);
	// win = SDL_CreateWindow("sw test",SDL_WINDOWPOS_UNDEFINED,SDL_WINDOWPOS_UNDEFINED,64,64,SDL_WINDOW_SHOWN);
	// screen = SDL_GetWindowSurface(win);
	
	// canvas = SDL_LoadBMP("bg32.bmp");
	
	// SDL_BlitSurface(canvas,NULL,screen,NULL);
	// SDL_UpdateWindowSurface(win);
	
	
	// SDL_Delay(5000);
	// SDL_FreeSurface(canvas);
	
	
	// SDL_DestroyWindow(win);
	
	// SDL_Quit();
	
// }